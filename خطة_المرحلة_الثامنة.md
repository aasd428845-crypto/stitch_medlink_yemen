# خطة المرحلة الثامنة — تقييم السائق + الدعم الفني

**الحالة:** 📋 مخطّطة (لم تبدأ — هذا ملف تخطيط فقط، لا بناء)
**الأولوية:** متوسطة — لا تحجب أي تدفق أساسي حالياً، لكنها تُكمل تجربة إنهاء الطلب للعميل

---

## الهدف

بعد أن يستلم العميل طلبه (`status = 'delivered'`)، يستطيع تقييم السائق (1–5 نجوم + تعليق اختياري)، ويرى مدير الفرع ومدير الشركة متوسط تقييمات كل سائق. بالتوازي، تُضاف شاشة "المساعدة والدعم" مشتركة بين كل الأدوار الثلاثة (عميل، مدير فرع، سائق) لعرض الأسئلة الشائعة وطرق التواصل المباشر.

---

## ما هو جاهز مسبقاً في قاعدة البيانات (لا حاجة لتعديله)

جدول `public.driver_ratings` موجود بالفعل منذ Migration `0003_bonus_commission_approval_notifications.sql`:

```sql
create table public.driver_ratings (
  id uuid primary key default gen_random_uuid(),
  order_id uuid not null references public.orders(id),
  driver_id uuid not null references public.users(id),
  client_id uuid not null references public.users(id),
  rating int not null check (rating between 1 and 5),
  comment text,
  created_at timestamptz not null default now(),
  unique(order_id)   -- طلب واحد = تقييم واحد كحد أقصى
);
```

مع سياسات RLS جاهزة:
- `ratings_client_insert`: العميل فقط يُدرج تقييماً باسمه (`client_id = auth.uid()`).
- `ratings_select`: العميل صاحب التقييم، أو السائق المُقيَّم، أو مدير الفرع/الشركة يرون التقييم.

⚠️ **لا حاجة لإنشاء Migration جديد لهذا الجزء** — الجدول والسياسات كافية كما هي. القيد `unique(order_id)` يمنع تلقائياً تكرار التقييم لنفس الطلب (نستخدمه للتحقق في الواجهة بدل استعلام إضافي).

---

## الجزء الأول: تقييم السائق

### أين يظهر خيار التقييم؟
في `OrderDetailScreen` (شاشة تفاصيل الطلب لدى العميل، `lib/screens/client/order_detail_screen.dart`):
- إذا كانت حالة الطلب `delivered` **ولا يوجد تقييم مسجَّل لهذا الطلب بعد** → يظهر زر "قيّم السائق".
- إذا كان التقييم موجوداً بالفعل → تُعرض النجوم والتعليق كعنصر للقراءة فقط (بدون إمكانية تعديل، لأن القيد `unique(order_id)` يمنع الإدراج مرة ثانية أصلاً).

### شاشة/نافذة التقييم (`RateDriverSheet`)
`BottomSheet` تحتوي:
- 5 نجوم قابلة للنقر (`rating` بين 1–5، إلزامي).
- حقل تعليق نصي متعدد الأسطر (اختياري).
- زر "إرسال التقييم" — مُعطَّل حتى يُختار تقييم واحد على الأقل.
- عند الإرسال: استدعاء `OrderService.submitDriverRating(...)` → عرض رسالة نجاح **فقط بعد تأكيد الإدراج فعلياً في Supabase** (لا رسالة وهمية).

### عرض متوسط التقييم لدى مدير الفرع
في `branch_drivers_tab.dart` (تبويب السائقين الحالي لدى مدير الفرع): إضافة شارة صغيرة بجانب كل سائق تعرض متوسط تقييمه (⭐ 4.6) وعدد التقييمات، محسوبة عبر استعلام تجميعي (`avg(rating)`, `count(*)`) على `driver_ratings` مفلترة بـ `driver_id`.

### عرض تقييم السائق لنفسه
في `driver_earnings_tab.dart` أو بطاقة إحصائية جديدة أعلى تبويب "أرباحي": متوسط تقييمه الشخصي + عدد التقييمات المُستلمة، حتى يعرف أداءه.

---

## الجزء الثاني: المساعدة والدعم

### نطاق هذه المرحلة (مبسَّط، بدون جدول جديد)
لا يوجد في `CLAUDE.md` تفصيل لنموذج "تذاكر دعم" (support tickets)، لذلك نطاق المرحلة يقتصر على:
- شاشة "المساعدة والدعم" (`HelpSupportScreen`) — مشتركة بين الأدوار الثلاثة.
- قسم أسئلة شائعة (FAQ) بنمط Accordion — **محتوى ثابت حسب الدور** (أسئلة العميل تختلف عن أسئلة السائق ومدير الفرع)، لكن النصوص كلها عبر ARB (لا نص مكتوب مباشرة).
- قسم "تواصل معنا": رقم هاتف/واتساب قابل للنقر (فتح تطبيق الاتصال أو واتساب عبر `url_launcher` الموجودة أصلاً في `pubspec.yaml`)، وبريد إلكتروني للدعم.

> إذا احتاج المستخدم لاحقاً نظام "تذاكر دعم" حقيقي (فتح شكوى، متابعة حالتها، رد الإدارة) فهذا يتطلب جدولاً جديداً (`support_tickets`) و**يُقترح كمرحلة منفصلة لاحقة**، وليس ضمن هذه المرحلة.

### نقطة الوصول
إضافة أيقونة "مساعدة" (`Icons.help_outline_rounded`) بجانب جرس التنبيهات في `RoleAppBarActions` (`lib/screens/shared/coming_soon_scaffold.dart`) — بذلك تظهر تلقائياً في شريط التطبيق لدى العميل ومدير الفرع والسائق دون تكرار الكود.

---

## الملفات المطلوب إنشاؤها

```
medlink_app/lib/screens/shared/
└── help_support_screen.dart         ← جديد: شاشة الأسئلة الشائعة + التواصل، تستقبل الدور الحالي لعرض أسئلة مناسبة

medlink_app/lib/widgets/
├── rate_driver_sheet.dart           ← جديد: BottomSheet لاختيار النجوم + التعليق + الإرسال
├── driver_rating_badge.dart         ← جديد: شارة (⭐ 4.6 · 12 تقييم) تُستخدم في تبويب سائقي مدير الفرع وتبويب أرباحي لدى السائق
└── faq_tile.dart                    ← جديد: عنصر Accordion لسؤال/جواب واحد
```

---

## الملفات المطلوب تعديلها

| الملف | التعديل |
|-------|---------|
| `lib/services/order_service.dart` | إضافة `submitDriverRating({orderId, driverId, rating, comment})` و`fetchRatingForOrder(orderId)` |
| `lib/services/order_controller.dart` | إضافة حالة تقييم الطلب الحالي (تحميل/إرسال) |
| `lib/services/branch_service.dart` | إضافة `fetchDriverRatingSummary(driverId)` → `{average, count}` |
| `lib/services/driver_orders_service.dart` | إضافة `fetchMyRatingSummary()` → متوسط + عدد تقييمات السائق الحالي |
| `lib/screens/client/order_detail_screen.dart` | عرض زر/نتيجة التقييم حسب حالة الطلب |
| `lib/screens/branch_manager/branch_drivers_tab.dart` | عرض `DriverRatingBadge` بجانب كل سائق |
| `lib/screens/driver/driver_earnings_tab.dart` | عرض `DriverRatingBadge` الخاص بالسائق نفسه أعلى الصفحة |
| `lib/screens/shared/coming_soon_scaffold.dart` (`RoleAppBarActions`) | إضافة أيقونة المساعدة بجانب الجرس، تفتح `/help` |
| `lib/routing/app_router.dart` | إضافة مسار مشترك `GET /help` |
| `lib/main.dart` | لا حاجة لـ Provider جديد إذا استُخدمت الخدمات الحالية (`OrderService`, `BranchService`, `DriverOrdersService`) — تحقق فقط أنها مُمرَّرة بالفعل (هي كذلك) |
| `lib/l10n/app_ar.arb` / `app_en.arb` | إضافة كل نصوص التقييم والمساعدة |

---

## النصوص العربية المطلوبة (تقريبية)

```json
"rateDriverTitle": "قيّم السائق",
"rateDriverPrompt": "كيف كانت تجربتك مع السائق؟",
"rateDriverCommentHint": "اكتب تعليقك (اختياري)",
"rateDriverSubmit": "إرسال التقييم",
"rateDriverSuccess": "شكراً لتقييمك",
"rateDriverAlreadyRated": "تم تقييم هذا الطلب مسبقاً",
"ratingAverageLabel": "متوسط التقييم",
"ratingCountLabel": "عدد التقييمات",
"noRatingsYet": "لا توجد تقييمات بعد",
"helpSupportTitle": "المساعدة والدعم",
"helpFaqSection": "الأسئلة الشائعة",
"helpContactSection": "تواصل معنا",
"helpCallUs": "اتصل بنا",
"helpWhatsapp": "راسلنا عبر واتساب",
"helpEmailUs": "راسلنا عبر البريد الإلكتروني"
```

---

## استعلامات Supabase المطلوبة

### `OrderService.submitDriverRating(...)`
```dart
await supabase.from('driver_ratings').insert({
  'order_id': orderId,
  'driver_id': driverId,
  'client_id': supabase.auth.currentUser!.id, // لا يُرسل من الواجهة، يُشتق من الجلسة
  'rating': rating,
  'comment': comment,
});
// يعتمد على قيد unique(order_id) لمنع التكرار من قاعدة البيانات نفسها كخط دفاع أخير
```

### `OrderService.fetchRatingForOrder(orderId)`
```sql
select * from driver_ratings where order_id = :orderId limit 1;
```

### `BranchService.fetchDriverRatingSummary(driverId)` / `DriverOrdersService.fetchMyRatingSummary()`
```sql
select avg(rating) as average, count(*) as total
from driver_ratings
where driver_id = :driverId;
```

---

## الأمان والـ RLS

لا حاجة لسياسات جديدة — الموجودة تكفي:

| التهديد | الحماية الحالية |
|---------|-----------------|
| عميل يُقيّم سائقاً لطلب ليس له | `ratings_client_insert`: `client_id = auth.uid()` في `with check` |
| عميل يُقيّم نفس الطلب مرتين | القيد `unique(order_id)` على مستوى قاعدة البيانات (رفض تلقائي، تُعرض رسالة "تم تقييم هذا الطلب مسبقاً" عند التقاط الخطأ) |
| سائق يرى تقييمات سائق آخر | `ratings_select` تمنع ذلك ضمنياً — السائق لا يُطابق شرط `driver_id = auth.uid()` لتقييمات ليست له |
| عميل يعدّل/يحذف تقييماً بعد إرساله | لا توجد سياسة `update`/`delete` لدور `client` على الجدول أصلاً، فالتعديل مرفوض تلقائياً من RLS |

---

## اختبار المرحلة الثامنة (بعد البناء)

**قبل البدء:**
1. طلب فعلي وصل لحالة `delivered` (من المرحلة 3 + 4 + 6).

**التحقق:**
- [ ] العميل يرى زر "قيّم السائق" فقط في الطلبات المُسلَّمة التي لم تُقيَّم بعد.
- [ ] إرسال تقييم (نجوم + تعليق) ينجح ويُخزَّن فعلياً في `driver_ratings` (تحقّق من جدول Supabase مباشرة).
- [ ] محاولة فتح شاشة التقييم لطلب مُقيَّم مسبقاً تعرض التقييم الموجود بدل نموذج إدخال جديد.
- [ ] مدير الفرع يرى متوسط تقييم صحيح لكل سائق في تبويب السائقين.
- [ ] السائق يرى متوسط تقييمه الشخصي في تبويب أرباحي.
- [ ] أيقونة "مساعدة" تظهر في شريط التطبيق لكل الأدوار الثلاثة وتفتح شاشة الأسئلة الشائعة الصحيحة حسب الدور.
- [ ] روابط الاتصال/واتساب/البريد تفتح فعلياً (تحقّق يدوي على الويب أو الجوال).

---

## التبعيات

**يجب أن تكون جاهزة قبل المرحلة 8:**
- ✅ المراحل 1–7 مكتملة (تم التأكيد في تقارير `المرحلة_السادسة.md` و`المرحلة_السابعة.md`).
- ⚠️ وجود طلب واحد على الأقل بحالة `delivered` لاختبار تدفق التقييم.

**المرحلة 8 لا تُعدّ شرطاً مسبقاً صارماً لأي مرحلة لاحقة**، لكنها منطقياً تُفضَّل قبل المرحلة 9 (الدردشة الحية + الموقع المباشر) لأن كلتيهما تُضيفان عناصر تفاعل بعد التسليم.
</content>
