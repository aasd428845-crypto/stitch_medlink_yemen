# تقرير المرحلة السادسة — واجهة السائق

**التاريخ:** 5 أغسطس 2026
**الحالة:** ✅ مكتملة (باستثناء عرض الخريطة المضمّنة — مؤجَّل عمداً بانتظار مفتاح Google Maps API)

---

## ملخص تنفيذي

المرحلة السادسة تُمكّن السائق من رؤية طلباته المُسندة، تحديث حالتها خطوة بخطوة (بدء التوصيل → تأكيد التسليم)، ومتابعة أرباحه الشهرية من العمولات. كل الاستعلامات تمر عبر `DriverOrdersService`، والتحديثات الحساسة لحالة الطلب تمر عبر دالة قاعدة بيانات آمنة (`security definer`) بدل التحديث المباشر.

> **ملاحظة مهمة:** عند بدء مراجعة هذه المرحلة، كان تطبيق Flutter متوقفاً عن العمل بالكامل بسبب تلف في ذاكرة التخزين المؤقت لحزم Dart (pub-cache) — مشكلة بيئية غير متعلقة بالكود. تم إصلاحها (`rm -rf` لذاكرة التخزين المؤقت + `flutter pub get` من جديد)، وتم التأكد من أن `flutter analyze` يعطي **`No issues found!`** والتطبيق يعمل بشكل طبيعي.

---

## الملفات المُنشأة

### 1. `lib/services/driver_orders_service.dart`
طبقة البيانات الخاصة بدور السائق:
- `fetchMyOrders()`: يجلب الطلبات المُسندة للسائق الحالي (assigned/in_progress/delivered) مع Join لعنوان التسليم، بيانات العميل، والبنود، ثم يرتّبها حسب الأولوية (المُسندة أولاً).
- `advanceOrderStatus(orderId, newStatus)`: يستدعي دالة RPC `driver_advance_order_status` بدل تحديث الجدول مباشرة.
- `fetchMyEarnings(month, year)`: يجلب عمولات السائق (`driver_commissions`) ضمن شهر مُحدَّد، مع مبلغ الطلب المرتبط.

### 2. `lib/services/driver_orders_controller.dart`
`ChangeNotifier` يدير حالة تابين: الطلبات (قائمة + فلترة بالحالة) والأرباح (قائمة + شهر/سنة مُختارة + إجمالي محسوب محلياً).

### 3. الشاشات — `lib/screens/driver/`
- **`driver_orders_tab.dart`**: قائمة الطلبات مع رقائق فلترة (الكل/مُسند/جاري التوصيل/تم التسليم)، سحب للتحديث، وبطاقة لكل طلب تعرض: رقم الطلب، شارة الحالة، اسم العميل، العنوان، **وقت التسليم المتوقع (إن وُجد)**، عدد البنود، والمجموع.
- **`driver_order_detail_screen.dart`**: بيانات العميل (مع زر اتصال هاتفي مباشر عبر `url_launcher`)، عنوان التسليم **ووقت التسليم المتوقع**، قائمة البنود (مع شارة "هدية" للبنود المجانية)، المجموع، الملاحظات، وزر تقدّم الحالة في الأسفل.
- **`driver_earnings_tab.dart`**: مرشّح شهري (هذا الشهر/الشهر الماضي)، بطاقتا إجمالي (الأرباح + عدد التوصيلات)، وقائمة عمولات بلون يميّز "مدفوعة" (أخضر) عن "معلّقة" (برتقالي).

### 4. `supabase/migrations/0006_driver_rls.sql`
- سياسة `orders_driver_select`: السائق يرى طلباته المُسندة فقط (`assigned_driver_id = auth.uid()`).
- سياسة `users_driver_select_client_info`: السائق يرى بيانات العميل (اسم/هاتف) لطلباته فقط.
- دالة `driver_advance_order_status()` بصلاحية `security definer`: تتحقق أن المستدعي هو السائق المُسند فعلاً، وأن الانتقال مسموح به (`assigned → in_progress` أو `in_progress → delivered`) قبل تنفيذ التحديث — **تصميم أكثر أماناً من التحديث المباشر** لأنه يمنع تعديل أي حقل آخر في الطلب.
- ملاحظة: سياسة قراءة العمولات الخاصة بالسائق كانت موجودة مسبقاً في Migration `0003` (`commissions_own_or_director`)، فلم تكن هناك حاجة لتكرارها.

---

## الملفات المُعدَّلة

| الملف | التعديل |
|-------|---------|
| `lib/screens/driver/driver_home_shell.dart` | استُبدل الهيكل الفارغ (`ComingSoon`) بثلاثة تبويبات حقيقية: طلباتي، أرباحي، دردشة (placeholder). |
| `lib/routing/app_router.dart` | أُضيف مسار متداخل `/driver/order/:id`. |
| `lib/main.dart` | أُضيف `DriverOrdersService` و`DriverOrdersController` كـ Providers. |
| `lib/l10n/app_ar.arb` / `app_en.arb` | أُضيفت مفاتيح واجهة السائق (فلاتر، تفاصيل الطلب، الأرباح)، بما فيها `driverScheduledDelivery` المُضافة اليوم. |

---

## فجوات تم اكتشافها وإصلاحها اليوم (أثناء المراجعة)

خطة المرحلة (`خطة_المرحلة_السادسة.md`) كانت تطلب عرض **وقت التسليم المتوقع** (`scheduled_delivery_at`)، وكان الحقل موجوداً في نموذج البيانات لكنه لم يكن معروضاً في أي شاشة. تم إصلاح ذلك بإضافة:
- سطر "وقت التسليم المتوقع" في بطاقة الطلب ضمن تاب "طلباتي".
- سطر معلومات مماثل في شاشة تفاصيل الطلب (ضمن قسم عنوان التسليم).

---

## فجوة متبقية (مؤجَّلة بقرار من المستخدم)

**عرض الخريطة المضمّنة**: عنوان المهمة الأصلية يذكر "عرض الخريطة" كجزء من واجهة السائق. الحزمة `google_maps_flutter` مثبّتة في `pubspec.yaml` وقاعدة البيانات تحتوي على `latitude`/`longitude` لكل عنوان توصيل، لكن لا توجد أي شاشة تستخدمها حالياً. **تم تأجيل هذا الجزء بطلب المستخدم** إلى حين توفر مفتاح Google Maps API — سيُبنى لاحقاً ضمن نفس المهمة الموجودة في قائمة المهام ("واجهة السائق: التوصيلات، الخريطة، الأرباح").

---

## الأمان

| التهديد | الإجراء المُتخذ |
|---------|----------------|
| سائق يرى طلبات سائق آخر | RLS: `assigned_driver_id = auth.uid()` على `orders` |
| سائق يُعدّل حقلاً غير `status` (مثل `total_amount` أو `assigned_driver_id`) | لا صلاحية UPDATE مباشرة؛ التعديل الوحيد المسموح هو عبر `driver_advance_order_status()` التي تُحدّث `status` فقط |
| سائق يقفز حالة غير منطقية (مثل `assigned → delivered` مباشرة) | الدالة تتحقق من الانتقال المسموح قبل التنفيذ وترفض غير ذلك بـ `raise exception` |
| سائق يرى بيانات عميل لا علاقة له بطلبه | سياسة `users_driver_select_client_info` مقيَّدة بدور `driver` وتكشف فقط صفوف `role = 'client'` |

---

## التحقق الذي تم إجراؤه

- `flutter analyze` → **`No issues found!`**
- `flutter gen-l10n` نُفِّذ بعد إضافة المفاتيح الجديدة.
- تشغيل التطبيق والتأكد من عدم وجود أخطاء تجميع (بعد إصلاح مشكلة pub-cache).

**لم يُجرَ بعد:** اختبار يدوي كامل بحساب سائق حقيقي (تسجيل دخول، رؤية طلب مُسند فعلياً، تقدّم الحالة، ظهور العمولة) — يتطلب بيانات تجريبية حقيقية (سائق + طلب مُسنَد له من لوحة مدير الفرع).
