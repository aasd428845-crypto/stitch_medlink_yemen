# المرحلة الثامنة — تقييم السائق + الدعم الفني

**الحالة:** ✅ مكتملة  
**التاريخ:** 2026-08-05

---

## ملخص ما تم بناؤه

بُنيت في هذه المرحلة ميزتان رئيسيتان: **تقييم السائق بعد التسليم** و**شاشة المساعدة والدعم المشتركة بين جميع الأدوار**.

---

## الجزء الأول: تقييم السائق

### كيف يعمل التدفق؟
1. العميل يفتح طلباً وصل إلى حالة `delivered`.
2. يظهر زر **"قيّم السائق"** في أسفل تفاصيل الطلب — يظهر فقط إذا لم يُقيَّم الطلب بعد.
3. العميل يضغط الزر فيفتح **RateDriverSheet** (Bottom Sheet) يحتوي على:
   - 5 نجوم قابلة للنقر (إلزامي اختيار نجمة واحدة على الأقل لتفعيل زر الإرسال).
   - حقل تعليق نصي متعدد الأسطر (اختياري).
   - زر **"إرسال التقييم"** — مُعطَّل تلقائياً حتى يُختار تقييم.
4. بعد الإرسال الناجح يُعرض التقييم للقراءة فقط (نجوم + تعليق) بدلاً من الزر — لا يمكن تعديله لأن قاعدة البيانات تمنع ذلك بـ RLS.
5. إذا حاول إرسال تقييم لطلب مُقيَّم مسبقاً تظهر رسالة "تم تقييم هذا الطلب مسبقاً".

### متوسط التقييم لدى مدير الفرع
في تبويب **السائقين** بلوحة مدير الفرع، أُضيفت **شارة التقييم** (`DriverRatingBadge`) أسفل اسم كل سائق تعرض:
- `⭐ 4.6 · 12` (متوسط · عدد التقييمات) إذا وُجدت تقييمات.
- `"لا توجد تقييمات"` إذا لم يُقيَّم السائق بعد.

### تقييم السائق لنفسه
في تبويب **"أرباحي"** للسائق، أُضيفت بطاقة **ملخص التقييم** أعلى القائمة تعرض:
- متوسط تقييمه الشخصي (مثال: `4.3 / 5.0`)
- عدد التقييمات التي حصل عليها

---

## الجزء الثاني: المساعدة والدعم

### شاشة `HelpSupportScreen`
شاشة مشتركة بين الأدوار الثلاثة (عميل، مدير فرع، سائق) تتكون من قسمين:

**أسئلة شائعة (FAQ)**  
عناصر Accordion (تتوسّع/تُطوى عند الضغط) — 5 أسئلة مخصَّصة لكل دور:
- **العميل:** كيف أطلب؟ كيف أتتبع طلبي؟ هل التوصيل مجاني؟ كيف أقيّم السائق؟ هل يمكن إلغاء الطلب؟
- **مدير الفرع:** تعيين السائق، إضافة سائق، المخزون، قراءة التقييمات، تعليق حساب.
- **السائق:** قبول الطلب، تأكيد التسليم، حساب العمولة، رؤية التقييمات، التعامل مع عميل غائب.

**تواصل معنا**  
ثلاثة روابط قابلة للنقر تفتح التطبيق الخارجي المناسب:
- 📞 **اتصل بنا** — يفتح تطبيق الهاتف
- 💬 **واتساب** — يفتح WhatsApp
- 📧 **البريد الإلكتروني** — يفتح تطبيق البريد

### نقطة الوصول
أُضيفت أيقونة `help_outline_rounded` في **`RoleAppBarActions`** (شريط التطبيق المشترك) بجانب جرس التنبيهات — تظهر تلقائياً لجميع الأدوار الثلاثة دون تكرار الكود.

---

## الملفات المُنشأة

| الملف | الوصف |
|-------|-------|
| `lib/widgets/rate_driver_sheet.dart` | Bottom Sheet للتقييم (نجوم + تعليق + إرسال) |
| `lib/widgets/driver_rating_badge.dart` | شارة التقييم المدمجة (⭐ avg · count) |
| `lib/widgets/faq_tile.dart` | عنصر Accordion لسؤال/جواب واحد |
| `lib/screens/shared/help_support_screen.dart` | شاشة المساعدة والدعم (FAQ + تواصل) |

---

## الملفات المُعدَّلة

| الملف | التعديل |
|-------|---------|
| `lib/services/order_service.dart` | + `submitDriverRating()` + `fetchRatingForOrder()` |
| `lib/services/branch_service.dart` | + `fetchDriverRatingSummary(driverId)` |
| `lib/services/driver_orders_service.dart` | + `fetchMyRatingSummary()` |
| `lib/services/driver_orders_controller.dart` | + حالة التقييم + `loadMyRatingSummary()` |
| `lib/screens/client/order_detail_screen.dart` | عرض زر التقييم أو النتيجة حسب حالة الطلب |
| `lib/screens/branch_manager/branch_drivers_tab.dart` | شارة التقييم بجانب كل سائق |
| `lib/screens/driver/driver_earnings_tab.dart` | بطاقة ملخص التقييم أعلى الصفحة |
| `lib/screens/shared/coming_soon_scaffold.dart` | أيقونة المساعدة في `RoleAppBarActions` |
| `lib/routing/app_router.dart` | مسار `/help` المشترك |
| `lib/l10n/app_ar.arb` | 17 نص عربي جديد |
| `lib/l10n/app_en.arb` | 17 نص إنجليزي جديد |

---

## قاعدة البيانات

**لم يُنشأ أي Migration جديد** — الجدول `public.driver_ratings` وسياسات RLS كانت جاهزة منذ Migration `0003`:
- `ratings_client_insert`: يضمن أن العميل يُقيَّم فقط بـ `client_id = auth.uid()`.
- قيد `unique(order_id)`: يمنع تقييم نفس الطلب مرتين على مستوى قاعدة البيانات.
- `ratings_select`: يمنع السائق من رؤية تقييمات سائق آخر.

---

## نقاط الإطار المرجعي (للمراحل القادمة)

- **مسار المساعدة** `/help` يستقبل `UserRole` عبر `GoRouter extra` — أي دور جديد في المستقبل يُضاف بسطر واحد في `help_support_screen.dart`.
- **`DriverRatingBadge`** widget مستقل تماماً — يقبل `average` و`count` ويعرض أي من الحالات الثلاث (تحميل، لا تقييمات، مع تقييم).
- `flutter analyze` ✅ — صفر أخطاء أو تحذيرات.
