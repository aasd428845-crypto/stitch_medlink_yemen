# خطة التنفيذ المرجعية للمرحلة الخامسة: واجهة السائق

تغطي هذه الخطة بناء **المرحلة الخامسة** من تطبيق MedLink Yemen: واجهة **السائق** الكاملة (الطلبات المُسندة، عرض الخريطة، الأرباح).

**هذه خطة للمراجعة فقط — لم يبدأ التنفيذ بعد.**

---

## 📋 الأهداف والمتطلبات

1. **طلباتي (Assigned Deliveries)**:
   - عرض الطلبات المُسندة للسائق الحالي مع فلترة بالحالة (نشطة/مكتملة).
   - تفاصيل كل طلب: العميل، العنوان، البنود، المجموع.
   - تحديث حالة الطلب خطوة بخطوة (`assigned` → `in_progress` → `delivered`).
2. **عرض الخريطة (Map View)**:
   - عرض موقع عنوان التسليم على خريطة لكل طلب نشط (باستخدام `latitude`/`longitude` من `client_addresses`).
   - زر "فتح في تطبيق خرائط خارجي" (Google Maps) كبديل عملي بدون تبعية SDK خرائط مدفوعة.
3. **أرباحي (Earnings)**:
   - عرض قائمة العمولات (`driver_commissions`) الخاصة بالسائق: طلب، مبلغ، حالة الدفع (معلّق/مدفوع).
   - إجمالي الأرباح المعلّقة وإجمالي المدفوعة (محسوبة محلياً من نفس القائمة).

---

## ⚠️ قواعد معمارية إلزامية (`CLAUDE.md`)

- كل عملية Supabase تستخدم `AppConstants.supabaseDebugTag`.
- لا تكرار في منطق الأعمال — كل شيء عبر Service واحدة (`driver_service.dart`).
- `flutter analyze` يجب أن ينتهي بـ `No issues found!` قبل إغلاق المرحلة.
- لا حسابات وهمية — التحقق النهائي يتطلب حساب `driver` حقيقي بحالة `account_status = 'active'`.

---

## 🔍 ملاحظة على صلاحيات قاعدة البيانات

سياسة `orders_select` الحالية (من `0002`) تسمح للسائق برؤية الطلبات المُسندة له (`assigned_driver_id = auth.uid()`) — **لا حاجة لـ Migration جديدة** لهذا الجزء.

لكن لعرض اسم/هاتف العميل ضمن تفاصيل الطلب، السائق يحتاج قراءة صف العميل من `public.users` — نفس الفجوة التي عولجت لمدير الفرع في `0004_branch_manager_access.sql`. سنحتاج **إضافة شرط للسائق** ضمن نفس الملف (أو migration `0005` منفصل):

```sql
-- يُضاف كشرط OR إضافي داخل سياسة users_select الخاصة بمدير الفرع، أو كسياسة سائق مستقلة:
create policy "users_select_driver_own_order_clients"
  on public.users
  for select
  using (
    role = 'client'
    and exists (
      select 1 from public.orders o
      where o.client_id = public.users.id
        and o.assigned_driver_id = auth.uid()
    )
  );
```

هذا سيُدرج ضمن الخطة كـ `[NEW]` migration عند التنفيذ الفعلي.

---

## 🏗️ المكونات والملفات الجديدة والمعدلة (مقترحة)

### 1. الخدمة الجديدة — `lib/services/`

- **`[NEW]` `driver_service.dart`**:
  - `fetchAssignedOrders(driverId, {String? status})`: طلبات السائق مع Join للعميل والعنوان.
  - `fetchOrderDetailForDriver(orderId)`: تفاصيل كاملة (بنود + عميل + عنوان).
  - `updateOrderStatus(orderId, status)`: تقدّم الحالة (يعيد استخدام نفس نمط `BranchService.updateOrderStatus` دون تكراره — سيُدرس هل يُستخرَج كدالة مشتركة في مرحلة التنفيذ).
  - `fetchDriverCommissions(driverId)`: قائمة العمولات مع حالتها.

- **`[NEW]` `driver_controller.dart`** (`ChangeNotifier`):
  - يدير قوائم الطلبات النشطة/المكتملة والعمولات.
  - إجمالي الأرباح المعلّقة/المدفوعة محسوب من قائمة العمولات مباشرة.

### 2. الشاشات — `lib/screens/driver/`

- **`[MODIFY]` `driver_home_shell.dart`**: تفعيل الشاشات الحقيقية بدلاً من `ComingSoon` (سيبقى تبويب "الدردشة" كواجهة قيد الإنشاء إذا لم يكن ضمن نطاق هذه المرحلة — للتأكيد مع المستخدم).
- **`[NEW]` `driver_orders_tab.dart`**: قائمة الطلبات المُسندة مع فلترة (نشطة/مكتملة) وبطاقة لكل طلب.
- **`[NEW]` `driver_order_detail_screen.dart`**: تفاصيل الطلب + زر "فتح الموقع في خرائط جوجل" + زر تقدّم الحالة.
- **`[NEW]` `driver_earnings_tab.dart`**: قائمة العمولات + بطاقتا إجمالي (معلّق/مدفوع).

### 3. المكونات المشتركة — `lib/widgets/`

- **`[NEW]` `driver_order_card.dart`**: بطاقة طلب مُسند مع الحالة وزر فتح الخريطة.
- **إعادة استخدام**: `stat_card.dart` (من المرحلة الرابعة) لبطاقات إجمالي الأرباح، و`order_status_chip.dart` الحالي.

### 4. حزمة خارجية مطلوبة

- **`url_launcher`**: لفتح رابط خرائط جوجل (`https://maps.google.com/?q=lat,lng`) من التطبيق دون تضمين SDK خرائط كامل — الأنسب لتطبيق ويب/متعدد المنصات في هذه المرحلة. (سيُطلب تأكيد المستخدم إن كان يفضّل خريطة مضمّنة داخل التطبيق بدلاً من ذلك لاحقاً).

### 5. الترجمة ونظام التنقل

**مفاتيح جديدة في `app_ar.arb` / `app_en.arb`**: تبويبات (تم إدراج بعضها مسبقاً)، فلاتر الطلبات النشطة/المكتملة، "فتح الموقع"، "إجمالي معلّق"، "إجمالي مدفوع"، "مدفوعة"، "معلّقة".

**تحديثات `app_router.dart`**: `/driver/order/:id` ← تفاصيل الطلب للسائق.

**تحديث `main.dart`**: إضافة `DriverService` و`DriverController` لشجرة الـ Providers.

---

## 🛠️ خطة التحقق والاختبار

1. تنفيذ أي migration جديدة يدوياً على قاعدة البيانات الحقيقية قبل الاختبار.
2. `flutter gen-l10n` بعد تحديث ملفات ARB.
3. `dart run build_runner build --delete-conflicting-outputs` إن استُحدثت حقول جديدة.
4. `flutter analyze` للتأكد من **`No issues found!`**.
5. تحقق عملي بحساب سائق فعّال: ظهور الطلبات المُسندة، فتح الموقع في خرائط جوجل، تقدّم حالة الطلب، ظهور العمولات وصحة الإجماليات.

---

## ❓ نقاط تحتاج تأكيدك قبل البدء

1. هل خريطة جوجل الخارجية (رابط فتح تطبيق الخرائط) كافية، أم تريد خريطة مضمّنة داخل التطبيق (تتطلب مفتاح Google Maps API ورسوماً محتملة)؟
2. هل تبويب "الدردشة" (`driverChatLabel` الموجود حالياً في الشريط السفلي) ضمن نطاق هذه المرحلة، أم يبقى "قيد الإنشاء" ويُؤجَّل لمرحلة لاحقة؟
