# تقرير المرحلة السابعة — مركز التنبيهات + العروض الترويجية

**التاريخ:** 5 أغسطس 2026
**الحالة:** ✅ مكتملة ومُتحقَّق منها (كانت مبنية مسبقاً من جلسة سابقة؛ هذه الجلسة كانت مراجعة فحص شاملة + إصلاح ثغرتين وُجدتا)

---

## ملخص تنفيذي

المرحلة السابعة تُضيف مركز تنبيهات مشترك بين جميع الأدوار (عميل، مدير فرع، سائق) وعرض العروض الترويجية للعميل. عند بدء هذه الجلسة، طلب المستخدم التأكد من عدم وجود أي فجوة في هذه المرحلة قبل التقدّم للمرحلة الثامنة. تمت مراجعة كل طبقة (قاعدة البيانات → الخدمة → الـ Controller → الشاشات → التوجيه → الترجمة) وتبيّن أنها مبنية فعلياً وتعمل على بيانات حقيقية من Supabase، مع إصلاح ثغرتين تم اكتشافهما أثناء الفحص.

> **ملاحظة بيئية:** عند بدء المراجعة كان تطبيق Flutter متوقفاً عن العمل تماماً بسبب تلف في ذاكرة التخزين المؤقت للبناء (`.dart_tool` / `build`) — ظهرت أخطاء غريبة مثل `'Matrix4' isn't a type` و`Method not found: 'DateSymbols'` من داخل حزم Flutter نفسها وليس من كود المشروع. تم الإصلاح بـ `flutter clean` + حذف `.dart_tool`/`build` + `flutter pub get` من جديد، وأعيد تشغيل السيرفر بنجاح.

---

## ما تم التحقق منه (موجود ويعمل فعلياً)

### قاعدة البيانات
- `supabase/migrations/0002_catalog_inventory_orders.sql`: جدول `promotional_offers` مع سياسة قراءة للعروض الفعّالة فقط (`offers_select_active`) وسياسة إدارة لمدير الشركة (`offers_director_manage`).
- `supabase/migrations/0003_bonus_commission_approval_notifications.sql`: جدولا `notifications` (مع `target_role`, `target_branch_id`, `related_offer_id`) و`notification_reads` (لتتبّع القراءة لكل مستخدم)، مع RLS: `notifications_select_relevant` (تعرض فقط التنبيهات ذات الصلة بدور/فرع المستخدم) و`notification_reads_own` (كل مستخدم يرى ويُعدّل قراءاته فقط).

### الخدمات (Services) — مصدر واحد للمنطق
- **`lib/services/notification_service.dart`**: `fetchMyNotifications(branchId)` يجلب التنبيهات مع Join مع `notification_reads` لحساب `isRead` محلياً بدل استعلام إضافي، `markAsRead()` و`markAllAsRead()` بـ upsert دفعي. كل دالة مُسجَّلة عبر `SUPABASE_DEBUG` عند الفشل.
- **`lib/services/catalog_service.dart`**: `fetchActiveOffers(governorate)` يجلب العروض الفعّالة من `promotional_offers` مباشرة.

### الـ Controllers
- **`lib/services/notification_controller.dart`**: يحمّل التنبيهات تلقائياً عند تغيّر المستخدم المسجَّل دخوله (عبر الاستماع لـ `AuthController`)، يحسب `unreadCount` للشارة، ويطبّق تحديثاً تفاؤلياً (optimistic update) عند التعليم كمقروء مع التراجع التلقائي عند فشل الطلب.
- **`lib/services/catalog_controller.dart`**: `loadOffers()` يُحمَّل ضمن `initialize()` جنباً إلى جنب مع المنتجات والفئات.

### الشاشات والودجات
- **`lib/screens/shared/notifications_screen.dart`**: قائمة كاملة بحالات التحميل/الخطأ/الفارغة، زر "تعليم الكل كمقروء"، وسحب للتحديث.
- **`lib/widgets/notification_tile.dart`**: بطاقة تنبيه بأيقونة حسب الدور المستهدف، نقطة "غير مقروء"، وتنسيق نسبي للتاريخ (الآن / منذ كذا دقيقة / منذ كذا يوم).
- **`lib/widgets/offer_banner_card.dart`**: بطاقة أفقية للعرض تظهر في تبويب الرئيسية (`HomeTab`) بشريط عروض قابل للتمرير.
- **`lib/screens/client/offer_detail_screen.dart`**: صفحة تفاصيل كاملة للعرض (صورة بارزة، شارة الخصم، الوصف، تاريخ البداية/النهاية، المحافظة المستهدفة).
- **`lib/screens/shared/coming_soon_scaffold.dart`** (`RoleAppBarActions`): جرس تنبيهات مشترك بشارة عدد غير المقروء + زر تسجيل الخروج، يُستخدم في شريط التطبيق العلوي لكل الأدوار.

### التوجيه (Routing)
- `GET /notifications` مسار مشترك بين كل الأدوار.
- `GET /client/offer/:id` يستقبل كائن `PromotionalOffer` كاملاً عبر `extra` بدون إعادة استعلام.

### الترجمة (i18n)
- كل النصوص (`notificationsTitle`, `notificationsMarkAllRead`, `notificationsEmpty`, `offersSection`, `offerValidFrom`, `offerValidUntil`, `offerGovernorate`, ...) موجودة في `app_ar.arb` و`app_en.arb` بدون أي نص مكتوب مباشرة (hardcoded) في الشاشات الرئيسية.

---

## فجوتان تم اكتشافهما وإصلاحهما أثناء هذه المراجعة

### 1. غياب جرس التنبيهات في شاشة السائق
`driver_home_shell.dart` كان يحتوي فقط على زر تسجيل خروج في شريط التطبيق، بدون أي وصول لمركز التنبيهات — بعكس شاشتَي العميل ومدير الفرع اللتين تستخدمان `RoleAppBarActions`. **تم الإصلاح** باستبدال الزر المفرد بـ `RoleAppBarActions` نفسه، ليصبح السائق أيضاً قادراً على رؤية التنبيهات وعدّاد غير المقروء.

### 2. عطل حرج أقدم من هذه المرحلة: "إضافة إلى السلة" غير فعّالة فعلياً
أثناء تتبّع الاستدعاءات وُجد أن `CartController.addItem()` لم يكن يُستدعى من أي مكان في التطبيق إطلاقاً. الأزرار الثلاثة التالية كانت تعرض فقط رسالة نجاح وهمية (`"تمت الإضافة"`) دون أن تُضيف أي شيء فعلياً للسلة:
- `lib/screens/client/home_tab.dart` (زر الإضافة السريعة على بطاقة المنتج في الرئيسية)
- `lib/screens/client/catalog_tab.dart` (نفس الزر في شاشة الكتالوج الكامل)
- `lib/screens/client/product_detail_screen.dart` (زر "أضف إلى السلة" في صفحة تفاصيل المنتج)

هذا يخالف قاعدة "لا رسالة نجاح بدون نتيجة حقيقية مؤكدة" في `CLAUDE.md`، وكان يعني عملياً أن السلة (والطلبات المبنية عليها من المرحلة 3) لا يمكن الوصول إليها من الواجهة بتاتاً رغم أن منطقها مبني بالكامل. **تم الإصلاح** بربط الأزرار الثلاثة فعلياً بـ `context.read<CartController>().addItem(product)` قبل عرض رسالة النجاح.

---

## التحقق الذي تم إجراؤه

- `flutter clean` + `flutter pub get` لإصلاح كاش البناء التالف، ثم إعادة تشغيل السيرفر والتأكد من `HTTP 200`.
- `flutter analyze` → **`No issues found!`** (بعد كل التعديلات، بما فيها إزالة استيرادات أصبحت غير مستخدمة في `driver_home_shell.dart`).
- تتبّع يدوي لسلسلة الاستدعاءات الكاملة (قاعدة البيانات → Service → Controller → Screen → Router → l10n) لكل من التنبيهات والعروض للتأكد من عدم وجود بيانات وهمية أو مسارات مقطوعة.

**لم يُجرَ بعد:** اختبار يدوي فعلي داخل المتصفح بحساب مستخدم حقيقي (تسجيل دخول، وصول تنبيه فعلي من الإدارة، التحقق من ظهوره والتعليم عليه كمقروء) — يتطلب إدراج بيانات تجريبية في جدول `notifications` من لوحة Supabase.

---

## الخلاصة

المرحلة السابعة **مكتملة فعلياً بدون فجوات متبقية** بعد إصلاح الثغرتين أعلاه. تم أيضاً إصلاح عطل حرج في السلة كان موجوداً منذ مرحلة سابقة ولم يكن مرتبطاً بهذه المرحلة تحديداً، لكنه كان يمنع اختبار أي تدفق طلب فعلي من الواجهة. التطبيق جاهز للانتقال إلى المرحلة الثامنة (تقييم السائق + الدعم الفني).
