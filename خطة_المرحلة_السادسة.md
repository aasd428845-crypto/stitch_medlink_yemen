# خطة المرحلة السادسة — واجهة السائق

**الحالة:** 📋 مخطّطة (لم تبدأ)  
**الأولوية:** عالية — السائق حالياً يرى شاشة "قيد الإنشاء" بعد تسجيل الدخول

---

## الهدف

بناء واجهة السائق الكاملة: عرض الطلبات المُسندة، إدارة حالتها، وعرض الأرباح. الشاشة الحالية (`driver_home_shell.dart`) هي هيكل فارغ بثلاثة تبويبات — هذه المرحلة تملأها بمحتوى حقيقي من Supabase.

---

## الشاشات المطلوبة

### التبويب 1: طلباتي (`DriverOrdersTab`)
**وصف:** قائمة بجميع الطلبات المُسندة للسائق الحالي.

**حالات الطلب المرئية للسائق:**

| الحالة | ما يظهر | الإجراء المتاح |
|--------|---------|----------------|
| `assigned` | الطلب مُسنَد، لم يُبدأ بعد | زر "بدء التوصيل" |
| `in_progress` | جاري التوصيل | زر "تأكيد التسليم" |
| `delivered` | مكتمل | عرض فقط (بدون إجراء) |

**البيانات المعروضة لكل طلب:**
- رقم الطلب
- اسم العميل وهاتفه
- عنوان التسليم
- عدد البنود والمبلغ الكلي
- الوقت المُتوقع للتسليم (`scheduled_delivery_at`)
- شريحة الحالة بألوان

**التفاعل:**
- سحب للتحديث (Pull to Refresh)
- النقر على طلب → شاشة `DriverOrderDetailScreen`

---

### شاشة تفاصيل الطلب (`DriverOrderDetailScreen`)
**وصف:** عرض كامل لطلب مُسنَد مع إمكانية التحديث.

**المحتوى:**
- بيانات العميل (اسم + هاتف قابل للاتصال)
- عنوان التسليم الكامل
- قائمة المنتجات (اسم + كمية + سعر)
- المبلغ الكلي
- ملاحظات الطلب

**الأزرار:**
- "بدء التوصيل" (assigned → in_progress)
- "تأكيد التسليم" (in_progress → delivered)

---

### التبويب 2: أرباحي (`DriverEarningsTab`)
**وصف:** ملخص مالي مستخرج من جدول `driver_commissions` (Migration 0003).

**ما يُعرض:**
- إجمالي الأرباح للشهر الحالي
- عدد الطلبات المُنجزة
- قائمة مفصّلة: كل طلب مُسلَّم → المبلغ + العمولة + التاريخ
- مرشّح شهري (الشهر الحالي / الشهر الماضي)

**المصادر:**
- جدول `driver_commissions`: `driver_id`, `order_id`, `amount`, `created_at`
- جدول `orders`: تفاصيل الطلب المرتبط

---

### التبويب 3: الدردشة (`DriverChatTab`)
**وصف:** في هذه المرحلة — شاشة "قيد الإنشاء" مُحسّنة تُشير إلى المرحلة التاسعة.

> الدردشة الحية تتطلب Supabase Realtime وتُبنى في المرحلة التاسعة. في هذه المرحلة نضع placeholder واضح.

---

## الملفات المطلوب إنشاؤها

```
medlink_app/lib/screens/driver/
├── driver_home_shell.dart              ← تعديل: استبدال ComingSoonBody بالتبويبات الحقيقية
├── driver_orders_tab.dart              ← جديد: قائمة الطلبات
├── driver_order_detail_screen.dart     ← جديد: تفاصيل طلب + تغيير الحالة
└── driver_earnings_tab.dart            ← جديد: ملخص الأرباح

medlink_app/lib/services/
├── driver_orders_service.dart          ← جديد: استعلامات Supabase للسائق
└── driver_orders_controller.dart       ← جديد: ChangeNotifier لحالة التبويبات
```

---

## الملفات المطلوب تعديلها

| الملف | التعديل |
|-------|---------|
| `lib/main.dart` | إضافة `DriverOrdersService` و`DriverOrdersController` كـ Providers |
| `lib/l10n/app_ar.arb` | إضافة نصوص واجهة السائق |
| `lib/l10n/app_en.arb` | نفس النصوص بالإنجليزية |

---

## النصوص العربية المطلوبة

```json
"driverMyOrdersTitle": "طلباتي",
"driverNoOrders": "لا توجد طلبات مُسندة حالياً",
"driverStartDelivery": "بدء التوصيل",
"driverConfirmDelivery": "تأكيد التسليم",
"driverDeliveryStarted": "بدأت رحلة التوصيل",
"driverDeliveryConfirmed": "تم تسليم الطلب بنجاح",
"driverOrderDetailTitle": "تفاصيل الطلب",
"driverClientPhone": "هاتف العميل",
"driverDeliveryAddress": "عنوان التسليم",
"driverEarningsTitle": "أرباحي",
"driverEarningsThisMonth": "أرباح هذا الشهر",
"driverTotalDeliveries": "إجمالي التوصيلات",
"driverNoEarnings": "لا توجد أرباح مسجّلة بعد",
"driverOrderEarning": "عمولة الطلب",
"driverChatComingSoon": "الدردشة المباشرة ستكون متاحة قريباً"
```

---

## استعلامات Supabase المطلوبة

### `DriverOrdersService.fetchMyOrders()`
```sql
SELECT
  orders.*,
  client_addresses.*,
  users!orders_client_id_fkey (name, phone),
  order_items (*, products (name, unit_price))
FROM orders
WHERE assigned_driver_id = auth.uid()
  AND status IN ('assigned', 'in_progress', 'delivered')
ORDER BY
  CASE status
    WHEN 'assigned' THEN 1
    WHEN 'in_progress' THEN 2
    WHEN 'delivered' THEN 3
  END,
  created_at DESC;
```

### `DriverOrdersService.updateOrderStatus(orderId, status)`
```dart
// الانتقالات المسموح بها للسائق فقط:
// assigned → in_progress
// in_progress → delivered
await supabase.from('orders')
  .update({'status': status})
  .eq('id', orderId)
  .eq('assigned_driver_id', auth.uid());  // حماية: لا يعدّل طلبات الآخرين
```

### `DriverOrdersService.fetchMyEarnings(month)`
```sql
SELECT
  dc.amount,
  dc.created_at,
  orders.id as order_id,
  orders.total_amount
FROM driver_commissions dc
JOIN orders ON orders.id = dc.order_id
WHERE dc.driver_id = auth.uid()
  AND EXTRACT(MONTH FROM dc.created_at) = :month
  AND EXTRACT(YEAR FROM dc.created_at) = :year
ORDER BY dc.created_at DESC;
```

---

## الأمان والـ RLS

**سياسات جديدة مطلوبة في Migration 0006:**

```sql
-- السائق يرى طلباته المُسندة فقط
create policy "orders_driver_select"
  on public.orders for select
  using (assigned_driver_id = auth.uid());

-- السائق يُحدّث حالة طلبه فقط (assigned → in_progress → delivered)
create policy "orders_driver_update_status"
  on public.orders for update
  using (assigned_driver_id = auth.uid())
  with check (
    -- يمنع السائق من تعديل أي حقل غير status
    branch_id = (select branch_id from orders where id = orders.id),
    client_id = (select client_id from orders where id = orders.id),
    assigned_driver_id = auth.uid()
  );

-- السائق يرى عمولاته فقط
create policy "commissions_driver_select_own"
  on public.driver_commissions for select
  using (driver_id = auth.uid());
```

---

## اختبار المرحلة السادسة

**قبل البدء:**
1. أنشئ سائقاً عبر لوحة مدير الفرع (المرحلة الخامسة)
2. سجّل دخول بحساب السائق وغيّر كلمة المرور
3. سجّل دخول بحساب العميل وأنشئ طلباً
4. من لوحة مدير الفرع: اقبل الطلب وأسنده للسائق

**التحقق:**
- [ ] السائق يرى الطلب المُسنَد في تاب "طلباتي"
- [ ] الضغط على "بدء التوصيل" يُغيّر الحالة إلى `in_progress`
- [ ] العميل يرى الحالة "جاري التوصيل" في طلباته
- [ ] الضغط على "تأكيد التسليم" يُكمل الطلب
- [ ] تاب "أرباحي" يعرض العمولة المُضافة (إذا كانت `driver_commissions` تحتوي بيانات)
- [ ] السائق لا يرى طلبات السائقين الآخرين

---

## التبعيات

**يجب أن تكون جاهزة قبل المرحلة 6:**
- ✅ المرحلة 1–5 مكتملة
- ⚠️ Migration 0005 مُطبَّقة على Supabase (تحمي الـ RLS)
- ⚠️ وجود طلبات فعلية مُسنَدة لسائق لاختبار الواجهة

**المرحلة 6 تُعدّ شرطاً مسبقاً لـ:**
- المرحلة 8: تقييم السائق (يحتاج قائمة الطلبات المُسلَّمة)
- المرحلة 9: الدردشة الحية (يستخدم نفس هيكل التنقل)
