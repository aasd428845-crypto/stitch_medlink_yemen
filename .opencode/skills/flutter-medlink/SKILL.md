---
name: flutter-medlink
description: Build and maintain MedLink Yemen Flutter applications while preserving shared architecture and design contracts.
---

Inspect existing state management, routing, models, services and widgets before adding code. Reuse shared/domain contracts. Use Flutter/Dart tooling when available. Run formatter, analyzer and relevant tests. For cross-app changes, inspect every consumer.
